/*import React from 'react';
import 'rxjs/Rx';

const Asymptoms = ({ asymptom }) => {
    return (
        <div>
            <center><h1>Symptoms list</h1></center>
            {asymptom.map((asymptom) => (
                <div class="card-body" key="ID">
                    <h5 class="card-title">{asymptom.name}</h5>
                    <h6 class="card-subtitle mb-2 text-muted">{asymptom.name}</h6>
                    <p class="card-text">{asymptom.name}</p>
                </div>
            ))}
        </div>
    )
};

export default Asymptoms;*/